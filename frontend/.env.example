# Connection settings only.
# Factory name, grid size and blueprint image are configured at runtime in the
# Configuration page and stored in the database — not here.
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8000/ws
